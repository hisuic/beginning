#include<stdio.h>
int main(void)
{
	int a;
	int total;
	printf("Input number.");
	scanf("%d",&a);
	if (a != 0){
		total + a;
		printf("Total is %d\n",total);
		}else{
			printf("Total is %d\n",total);
		}
		return 0;
}