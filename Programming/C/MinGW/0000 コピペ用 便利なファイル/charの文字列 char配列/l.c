#include <stdio.h>

/*charの文字列はこのcharの配列文を使用して出力する。
「DNA」の部分を目的の文字列に変更して使えばいい。
配列名の「str1」も自由配列名に変更することができる。*/

int main(void) {
    char str1[] = "DNA";

    printf("%s\n", str1);

    return 0;
}